library(networktree)

load('data-clean/data-reduced.Rds')

netvars <- colnames(cnetdata)

load('data-clean/data-clean.Rds')


nt1 <- networktree(nodevars=data[,netvars],
                   splitvars=data[,c("ROW")],
                   transform='glasso')

plot(nt1,labels=netvars)


netvars <- c(colnames(netdata))

#ega <- EGA(data[,netvars],model='glasso',algorithm='louvain',plot.EGA = TRUE,uni.method = 'expand')

#comparetree(nt1, id1 = 3, id2 = 5, highlights = 1, plot = TRUE,labels=netvars)
  